name: Release Current Checkpoint

on:
  workflow_dispatch:
    inputs:
      notes:
        description: Optional release notes
        required: false
        default: ""

permissions:
  contents: write

jobs:
  release-current-checkpoint:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Package world saves
        run: python tools/worlds_tool.py package --out dist

      - name: Create checkpoint release
        env:
          NOTES_INPUT: ${{ github.event.inputs.notes }}
        run: |
          set -euo pipefail
          ts="$(date -u +'%Y-%m-%d-%H%M')"
          tag="checkpoint-all-worlds-${ts}"
          title="All Modded MC Worlds Checkpoint - $(date -u +'%Y-%m-%d %H:%M')"

          notes_file="$(mktemp)"
          if [ -n "${NOTES_INPUT}" ]; then
            printf '%s\n\n' "${NOTES_INPUT}" > "${notes_file}"
          fi
          {
            printf '%s\n' 'This checkpoint release was generated from the current repository state.'
            printf '\n'
            printf '%s\n' 'Includes:'
            printf '%s\n' '- Combined all-worlds archive'
            printf '%s\n' '- Per-pack main-world archives'
            printf '\n'
            printf '%s\n' 'Note: `extras/` content is excluded from packaging.'
          } >> "${notes_file}"

          gh release create "${tag}" \
            --title "${title}" \
            --notes-file "${notes_file}" \
            --latest=false \
            dist/all/*.zip \
            dist/worlds/*.zip
